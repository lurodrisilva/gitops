# ---
# apiVersion: aws.upbound.io/v1beta1
# kind: ProviderConfig
# metadata:
#   name: {{ .Values.providers_config.config.name }}
# spec:
#   credentials:
#     source: Secret
#     secretRef:
#       namespace: {{ .Values.namespace }}
#       name: {{ .Values.providers_config.secret.name }}
#       key: {{ .Values.providers_config.secret.key }}
---
apiVersion: azure.m.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: default
spec:
  credentials:
    source: SystemAssignedManagedIdentity
  subscriptionID: "<SUBSCRIPTION_ID>" # TODO: replace with actual value or inject via overlay
  tenantID: "<TENANT_ID>" # TODO: replace with actual value or inject via overlay
  clientID: "<CLIENT_ID>" # TODO: replace with actual value or inject via overlay